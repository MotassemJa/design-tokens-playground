{
  "$schema": "https://schema.tokenscript.dev.gcp.tokens.studio/api/v1/core/schema/0/",
  "name": "Clamp String",
  "description": "Returns a CSS clamp() string without forcing TokenScript to evaluate mixed-unit math.",
  "type": "function",
  "keyword": "clamp_string",
  "input": {
    "type": "object",
    "required": ["min", "preferred", "max"],
    "properties": {
      "min": {
        "type": "string",
        "description": "Minimum value, for example '1rem'."
      },
      "preferred": {
        "type": "string",
        "description": "Preferred expression, for example '0.95rem + 0.35vw'."
      },
      "max": {
        "type": "string",
        "description": "Maximum value, for example '1.25rem'."
      }
    }
  },
  "script": {
    "type": "https://schema.tokenscript.dev.gcp.tokens.studio/api/v1/core/tokenscript/0/",
    "script": "variable args: Dictionary = {input};\nvariable minValue: String = args.get(\"min\");\nvariable preferredValue: String = args.get(\"preferred\");\nvariable maxValue: String = args.get(\"max\");\n\nreturn \"clamp(\".concat(minValue).concat(\", \" ).concat(preferredValue).concat(\", \" ).concat(maxValue).concat(\")\");"
  }
}
